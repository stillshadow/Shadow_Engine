# TA-039 LearnOpenGL-to-PBR coverage and final post-process path

- Scope: complete the technical-art-facing render path through PBR and document the mapping to LearnOpenGL.
- Acceptance: normal strength and parallax controls reach HLSL; GBuffer is followed by SSAO; forward lighting writes a linear HDR target; Bloom and exposure are applied before swap-chain output; saved scene exposes the controls; coverage and portfolio documents identify exact gaps.
- Files: `src/Renderer/D3D12Renderer.*`, `src/Scene/Scene.*`, `src/Editor/EditorLayer.*`, `src/Scene/SceneSerializer.*`, `shaders/*.hlsl`, `CMakeLists.txt`, `assets/scenes/current.scene.json`, `docs/ShadowEngine-LearnOpenGL到PBR功能对照.md`, `docs/ShadowEngine-作品集验收.md`.
- Validation: `cmake --build --preset debug`; `ctest --test-dir build/vscode -C Debug --output-on-failure`; 10-second executable smoke run; manual PIX/RenderDoc inspection remains recommended.
- Known limits: procedural IBL is used instead of true cubemap convolution/prefiltering; point/area lights do not cast shadows; GLB import remains single mesh/single material.
