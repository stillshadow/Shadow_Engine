# TA-042 - Light viewport picking, single-step undo, and LearnOpenGL audit

- Status: Done (2026-08-14)
- Owner: Codex
- Scope: Make editable light visuals mouse-pickable, retain one undo snapshot for Scene edits, clarify Forward/Deferred output parity, and audit LearnOpenGL coverage against source code.
- Acceptance:
  - Point Light icons and Area Light rectangles can be selected from the viewport.
  - Light overlays take picking priority over object bounds.
  - Ctrl+Z and the Status-panel button undo the latest Scene-changing operation.
  - Continuous ImGui/Gizmo dragging is grouped into one undo operation.
  - The coverage document distinguishes complete, approximate, and missing LearnOpenGL topics.
  - Forward/Deferred documentation explains why matching Lit output is correct.
- Validation:
  - Debug build succeeds.
  - All CTest tests pass, including projected light-icon picking.
  - Runtime stays alive after startup.
  - Manual Windows UI automation was attempted but unavailable because the helper could not access its local Codex runtime directory (`EPERM`).
