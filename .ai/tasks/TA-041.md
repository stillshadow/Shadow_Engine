# TA-041 - One-click Forward and Deferred rendering comparison

- Status: Done (2026-08-14)
- Owner: Codex
- Scope: Replace the experimental hybrid reflection path with two readable raster pipelines that render the same scene and switch from one editor control.
- Acceptance:
  - Scene data persists a `Forward` or `Deferred` render path.
  - Forward renders objects directly into the HDR scene target.
  - Deferred first writes three GBuffer MRTs, then performs one fullscreen lighting pass.
  - Both paths share the same PBR material, scene lights, shadow map, HDR environment, Bloom, and Tone Mapping inputs.
  - GBuffer and SSAO debug views remain available.
  - Removed reflection experiments leave no source, shader, UI, serializer, documentation, or task-state remnants.
- Validation:
  - Debug build succeeds.
  - All automated tests pass.
  - Forward remains alive while skipping the GBuffer pass.
  - Deferred remains alive while executing the GBuffer pass.
  - Repository-wide obsolete-feature search returns no matches outside generated third-party/build metadata.
