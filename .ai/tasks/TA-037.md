# TA-037 - Portfolio diagnostic scene and capture workflow

- Status: Ready
- Owner: Codex + learner
- Scope: Use a self-contained glTF/GLB PBR asset to build a reproducible comparison scene for the two raster pipelines.
- Acceptance:
  - `assets/models/` contains a discoverable single-Mesh, single-material GLB.
  - The saved scene contains a main subject, ground, Directional light, and fill light.
  - Forward and Deferred can render the same scene and material inputs.
  - GBuffer, SSAO, HDR/Bloom, and Tone Mapping remain inspectable.
  - Build, tests, and runtime smoke checks pass.
- Validation:
  - `cmake --build --preset debug`
  - `ctest --test-dir build/vscode -C Debug --output-on-failure`
  - Switch both render paths in the editor and compare matching camera/light settings.
