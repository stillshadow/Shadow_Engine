# TA-043 - Complete remaining LearnOpenGL-through-PBR renderer features

- Status: Done (2026-08-14)
- Owner: Codex
- Scope: Implement every missing item recorded by the source audit: standard Split-Sum IBL, transparency, stencil outline, MSAA, GPU instancing, a useful Geometry Shader visualization, Spot Light, Point Shadow, and back-face culling.
- Completed:
  - Back-face culling in the main geometry PSO.
  - Spot Light scene data, persistence, editor creation/parameters, cone visualization, rotation Gizmo, and Forward/Deferred PBR evaluation.
  - CPU-precomputed Irradiance Cubemap, five-level GGX prefilter Cubemap, and BRDF integration LUT; both raster paths sample these resources.
  - First Point Light six-face linear-distance Cubemap shadow with four-sample filtering in both raster paths.
  - Sorted Forward transparency fallback shared by Forward and Deferred, with material opacity control.
  - D24S8 selected-object stencil mask and expanded orange outline.
  - Optional Forward 4x MSAA color/depth targets and HDR resolve; Deferred intentionally stays single-sample.
  - StructuredBuffer instance transforms and consecutive identical-mesh/material Draw batching through `SV_InstanceID`.
  - Optional Geometry Shader triangle-normal visualization for the selected object.
- Validation so far:
  - Each completed stage builds in Debug.
  - Split-Sum IBL remained alive for 20 seconds.
  - Point Shadow remained alive for 12 seconds after fixing a Deferred Root Signature parameter-count mismatch; no D3D12 Debug Layer errors remained.
  - Existing three CTest tests pass, including Spot Light serialization.
  - Transparency + stencil + MSAA + Geometry Shader stayed alive for 15 seconds with no fatal/debug-layer log.
  - Deferred instanced GBuffer stayed alive for 12 seconds.
  - A real two-object `DrawIndexedInstanced(..., 2, ...)` batch stayed alive for 12 seconds; the temporary object was removed afterward.
  - Final Debug build and all three CTest tests pass.
- Remaining: Manual visual inspection of the new switches and outlines, because the Windows UI helper cannot access its Codex runtime directory (`EPERM`).
- Documentation: Per user request, do not create per-feature course chapters during implementation. Produce unified learning material only after the renderer is stable and the user asks to learn it.
