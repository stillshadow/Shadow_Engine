# TA-034 - GBuffer and inspectable multi-pass foundation

Status: Implemented (2026-08-14)

## Scope

- Add three `R16G16B16A16_FLOAT` GBuffer targets.
- Compile `GBuffer.hlsl` and expose Base Color, Normal, and World Position debug views.
- Make RTV/SRV transitions explicit in `D3D12Renderer::Render`.

## Validation

- `cmake --build --preset debug`: passed.
- `ctest --test-dir build/vscode -C Debug --output-on-failure`: passed 3/3.
- Manual PIX/RenderDoc inspection: pending.
