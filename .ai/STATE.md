# Project state

- Current phase: Forward / Deferred portfolio comparison
- Active task: Handoff after completed renderer implementation
- Active owner: Released
- Starting commit: c2cdc53
- Current project state: TA-043 is complete. Forward and Deferred use standard CPU-precomputed Split-Sum IBL and share Directional, Point, Area, and Spot PBR lighting. The first Directional and first Point light cast shadows. The engine also has back-face culling, sorted Forward transparency fallback, selected-object stencil outline, optional Forward 4x MSAA resolve, real GPU instance batching, and a Geometry Shader normal debug view.
- Changes: Added Irradiance/Prefilter/BRDF LUT resources; Spot Light and Point Shadow; D24S8 stencil selection; opacity editing and mixed Deferred/Forward transparency; Forward-only MSAA; per-frame StructuredBuffer transforms with consecutive identical-material batching; and optional Geometry Shader triangle normals. ImGui now owns mouse input through `WantCaptureMouse` plus current-frame hover checks, preventing viewport click-through; Tab clears selection/highlight while retaining the full editor-overlay toggle. Ground outlines now depth-test against foreground meshes. The editor can create built-in Sphere, Cube, and Plane objects directly in the current Scene; built-in Cube/Plane meshes are generated by AssetManager. No new course chapters were created per user request.
- Validation: Final Debug build succeeds and all 3 CTest tests pass. Default, Deferred, MSAA/Geometry/Transparency, and real two-instance batch runs remained alive for 12-15 seconds without a fatal log. The Directional Shadow PSO D32 format mismatch and a Resize-only RTV heap overlap between the MSAA HDR target and Point Shadow face 0 were fixed. An automated Win32 test resized the running window to 1100x700, 1600x950, and 900x600; the process remained alive with no InfoQueue error log. Temporary validation scene changes were restored. `git diff --check` passes with only the existing README line-ending warning.
- Risks: Forward and Deferred intentionally produce nearly matching Lit output; their difference is pass organization and resource use. Transparent objects use the normal Forward fallback and distance sorting rather than order-independent transparency. Instance batching currently requires consecutive objects with identical asset/material values. Manual visual inspection of the new UI switches and primitive buttons is still required. Debug builds now drain `ID3D12InfoQueue` after initialization and each frame, turning Error/Corruption messages into descriptive logged exceptions instead of opaque debugger-only `0x87A` breaks.
- Relevant files: `src/Scene/Scene.h`, `src/Renderer/D3D12Renderer.cpp`, `shaders/MaterialPreview.hlsl`, `shaders/GBuffer.hlsl`, `shaders/DeferredLighting.hlsl`, `docs/ShadowEngine-前向与延迟渲染对比.md`, `assets/scenes/current.scene.json`.
- Next actions: On the next work session, open the app normally and visually check the primitive buttons, Tab input behavior, ground outline, Opacity, 4x MSAA, Geometry Shader Normals, and both render paths. Create the final portfolio capture/README and one unified learning document only when requested.
- Last handoff: Renderer implementation, Resize descriptor fix, ImGui input isolation, Tab deselection, ground outline depth fix, and built-in Sphere/Cube/Plane creation were completed and validated on 2026-08-14. Changes are being committed and pushed to `main` per user request; no further business edits are planned in this handoff.
